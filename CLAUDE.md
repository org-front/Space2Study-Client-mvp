@AGENTS.md

You are Claude Code in this repo. There is no Cursor Browser and no Bugbot. For live UI and HTTP (CORS, 401, Axios bodies) use the `chrome-devtools` MCP. On first run, approve project MCP servers from `.mcp.json` (`claude` interactive session). Do not add Playwright, Knip, or CodeRabbit MCP. Never put tokens in committed files.
